package com.mst.models.dto;

/** Presentation settings only; the server chooses the form, grid and user. */
public class InventoryGridColumn {
    public String key;
    public int width;
    public int position;
    public boolean visible;
}
