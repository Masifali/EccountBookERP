'use strict';
// The document-aging grid consumes procedure amounts and captions without recomputing aging.
(()=>{
const el=id=>document.getElementById(id),kind=document.body.dataset.agingKind;
let rows=[],lookup={},sequence=0,request,selectedRow=null;
const numbers=new Set(['1stInterval','2ndInterval','3rdInterval','Above','DueAmount']);
const columns=[['DocumentType','Document Type',70],['InvoiceNo','Doc No',60],['ManualNo','Manual No',100],['InvoiceDate','Invoice Date',85],['PartyBillDate','Supplier Bill Date',85],['InvoiceDueDate','Due Date',85],['AccountCode','Account Code',100],['1stInterval','',100],['2ndInterval','',100],['3rdInterval','',100],['Above','',100],['DueAmount','Due Amount',100],['Remarks','Remarks',350]];
if(kind==='customer')columns.splice(2,3);
const intervalStart=kind==='customer'?4:7;
const status=t=>el('status').textContent=t;
async function api(path,options){const response=await fetch('/api/accounts/document-aging/'+kind+path,options);if(!response.ok)throw Error('Unable to load report ('+response.status+'). The database error is recorded in the server log.');return response.json()}
function bind(id,data,label){const select=el(id),old=select.value;select.replaceChildren(new Option('',''));data.forEach(r=>select.add(new Option(r[label]??'',r.Id)));$('#'+id).val(old).trigger('change.select2')}
async function refresh(){try{lookup=await api('/lookups');bind('account',lookup.accounts,'AccountTitle');bind('custom',lookup.customGroups,'AcLookUpsDescription');bind('party',lookup.partyGroups,'Description');status('')}catch(e){status(e.message)}}
function ledger(r){return '/accounts/reports/general-ledger?'+new URLSearchParams({accountId:r.AccountId,fromDate:String(lookup.year.Start_Period).slice(0,10),toDate:el('asOnDate').value})}
function render(){const table=el('results'),head=table.tHead,body=table.tBodies[0];head.replaceChildren();body.replaceChildren();selectedRow=null;const captions=rows[0]||{};const captionKeys=['FirstIntervalCaption','SecondIntervalCaption','ThirdIntervalCaption','AboveIntervalCaption'];columns.slice(intervalStart,intervalStart+4).forEach((c,i)=>c[1]=captions[captionKeys[i]]||'');const hr=head.insertRow();columns.forEach(([key,label,width])=>{const th=document.createElement('th');th.textContent=label;th.style.width=width+'px';th.style.minWidth=width+'px';hr.append(th)});let lastParty,group;const term=el('search').value.toLowerCase();const filtered=rows.filter(r=>Object.values(r).some(v=>String(v??'').toLowerCase().includes(term)));for(const r of filtered){if(lastParty!==r.PartyName){lastParty=r.PartyName;group=body.insertRow();group.className='group';const td=group.insertCell();td.colSpan=columns.length;const button=document.createElement('button');button.textContent='− '+lastParty;button.setAttribute('aria-expanded','true');button.onclick=()=>{const collapsed=button.getAttribute('aria-expanded')==='true';button.setAttribute('aria-expanded',String(!collapsed));button.textContent=(collapsed?'+ ':'− ')+r.PartyName;let next=group;/* group reference is captured below */};const ownGroup=group;button.onclick=()=>{const collapse=button.getAttribute('aria-expanded')==='true';button.setAttribute('aria-expanded',String(!collapse));button.textContent=(collapse?'+ ':'− ')+r.PartyName;for(let next=ownGroup.nextElementSibling;next&&!next.classList.contains('group');next=next.nextElementSibling)next.hidden=collapse};td.append(button)}const tr=body.insertRow();tr.tabIndex=0;tr.onclick=()=>{body.querySelectorAll('.selected').forEach(x=>x.classList.remove('selected'));tr.classList.add('selected');selectedRow=r};tr.onkeydown=e=>{if(e.ctrlKey&&e.code==='Space'){e.preventDefault();location.href=ledger(r)}};for(const [k] of columns){const td=tr.insertCell();if(k==='AccountCode'){const a=document.createElement('a');a.textContent=r[k]??'';a.href=ledger(r);td.append(a)}else{let v=r[k];if(k==='InvoiceDate'||k==='PartyBillDate'||k==='InvoiceDueDate'){const s=String(v??'').slice(0,10);if(/^\d{4}-\d{2}-\d{2}$/.test(s)){const [y,m,d]=s.split('-');v=d+'-'+['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'][Number(m)-1]+'-'+y}}td.textContent=v??'';if(numbers.has(k))td.className='amount';if(k==='Remarks')td.className='remarks'}}}status(filtered.length+' documents');}
async function show(){const token=++sequence;request?.abort();request=new AbortController();rows=[];render();status('Loading…');const query=new URLSearchParams({asOnDate:el('asOnDate').value,agingDays:el('days').value||0,accountId:el('account').value||0,customGroupId:el('custom').value||0,partyGroupId:el('party').value||0});try{const data=await api('?'+query,{signal:request.signal});if(token!==sequence)return;rows=data;render()}catch(e){if(e.name!=='AbortError')status(e.message)}}
el('show').onclick=show;el('refresh').onclick=refresh;el('reset').onclick=()=>{++sequence;request?.abort();rows=[];el('days').value='';render();el('asOnDate').focus()};el('search').oninput=render;
el('print').onclick=()=>status(rows.length?'Crystal print layout verification is pending.':'Record Not Found For Display');
el('shortcuts').onclick=()=>{el('shortcutDialog').showModal()};el('closeShortcuts').onclick=()=>el('shortcutDialog').close();
document.addEventListener('keydown',e=>{if(e.ctrlKey){const key=e.key.toLowerCase(),actions={s:show,n:()=>el('reset').click(),r:refresh,p:()=>el('print').click(),f5:()=>el('asOnDate').focus(),arrowup:()=>el('asOnDate').focus(),arrowdown:()=>el('grid').focus()};if(actions[key]){e.preventDefault();actions[key]()}}});
const today=new Date();el('asOnDate').value=[today.getFullYear(),String(today.getMonth()+1).padStart(2,'0'),String(today.getDate()).padStart(2,'0')].join('-');$('select').select2({allowClear:true,placeholder:''});refresh();
})();
